syntax="proto3";
package comms;
service NeuroEvolution{
    rpc FetchEnvironmentStream (Command) returns (stream Environment);
}

message Command{
    string In = 1;
}

message Environment{
    string json_data=1;
    bool success=2;
    string message=3;
}
